Authentification:

->passport pour authentification
->hashing pour sauvegarde sécurisée de mdp
->bcrypt


Pour la creation des utilisateurs on aurait pu créer un service à part "createUser.dto" en y
précisant les variables utilisées pour en créer un, mais on a déjà user entity donc pas obligé.






Demander à fabien si:
-je devrais vraiment utiliser les DTO pour register, car l'utilisateur n'existe pas encore et s'il faudrait créer
un fichier .dto pour y mettre les informations de creation de compte ou on y accede par utilisateur
comme par exemple la fonction register avec auth.controller